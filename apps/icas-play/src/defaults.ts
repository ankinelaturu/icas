/**
 * @file CLI identity defaults. Do not infer these from `--url`.
 */

/** Catalog / enrollment default when the operator omits vendor, product, or tenant. */
export const DEFAULT_ICAS_IDENTITY = "icas-bank";
