# @icas/redactor

Independent configurable redaction for model boundaries, logs, errors, and persisted evidence.

Packaged profiles (`model`, `evidence`, `terminal`) do not share rules. A string that is not matched by the selected profile is left unchanged.
